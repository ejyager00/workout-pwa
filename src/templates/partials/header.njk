<header class="bg-gray-50 border-b border-gray-200 py-8">
  <div class="max-w-5xl mx-auto px-4">
    <h1 class="text-2xl font-bold text-gray-900">{{ pageTitle | default(title) }}</h1>
  </div>
</header>
